<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <!--buttons style used in the app-->
    <style name="btnStyle" parent="Base.Widget.AppCompat.Button">
        <item name="android:textColor">@color/btnTextColor</item>
        <item name="android:textSize">@dimen/btnTextSize</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/round_corner_yellow_btn</item>
    </style>

    <!--edit fields style used in the app-->
    <style name="editTextStyle" parent="Base.Widget.AppCompat.EditText">
        <item name="android:textColor">@color/colorWhite</item>
        <item name="android:textColorHint">@color/colorWhite</item>
        <item name="android:textSize">@dimen/edtTextSize</item>
        <item name="android:paddingLeft">@dimen/edtFieldsPaddingLR</item>
        <item name="android:paddingRight">@dimen/edtFieldsPaddingLR</item>
        <item name="android:paddingTop">@dimen/edtFieldsPadding</item>
        <item name="android:paddingBottom">@dimen/edtFieldsPadding</item>
        <item name="android:background">@drawable/round_corner_black_edtfield</item>
    </style>


</resources>
